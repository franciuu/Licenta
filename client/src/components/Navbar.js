import "../styles/Navbar.css"
const Navbar = () => {
  return (
    <header>
      <nav className="navbar">
        <button>Log out</button>
      </nav>
    </header>
  );
};

export default Navbar;
